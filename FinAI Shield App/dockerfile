# ========================
# СТАДИЯ 1: Сборка (Build Stage)
# Используем базовый образ Node.js для установки зависимостей и сборки проекта
# ========================
FROM node:20-alpine AS build

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем файлы манифеста для установки зависимостей
COPY package.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальной код проекта
COPY . .

# Выполняем продакшн-сборку проекта (создание оптимизированных статических файлов)
# Замените 'build' на команду, указанную в вашем package.json для сборки
RUN npm run build

# ========================
# СТАДИЯ 2: Запуск (Runtime/Serve Stage)
# Используем легковесный веб-сервер (Nginx) для отдачи статических файлов
# Это самый маленький и безопасный образ для продакшна
# ========================
FROM nginx:alpine

# Копируем собранные статические файлы из стадии 'build'
# Vite создает файлы в папке /app/build (согласно vite.config.ts)
COPY --from=build /app/build /usr/share/nginx/html

# Порт, который будет слушать Nginx
EXPOSE 80

# Команда запуска Nginx
CMD ["nginx", "-g", "daemon off;"]